export function InspectorSidebar() {
  return (
    <aside className="shell-sidebar shell-sidebar-inspector" aria-label="Inspector">
      <h2 className="shell-sidebar-title">Inspector</h2>
      <p className="shell-sidebar-empty">Select a clip to see its properties.</p>
    </aside>
  );
}
